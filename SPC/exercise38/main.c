#include <stdio.h>
#include <time.h>
#include <stdlib.h>

struct user_t
{
    char name[8];
    int id;
};

void fillUsers(void)
{
    srand(time(NULL));
    int random = rand;
    for (int i = 0; i < 5; i++)
    {
        struct user_t user
    }
}

int main()
{

    return 0;
}